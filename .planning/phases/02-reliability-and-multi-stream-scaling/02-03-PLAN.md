---
phase: 02-reliability-and-multi-stream-scaling
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/yowo/logging.py
  - src/yowo/engine.py
  - src/yowo/metrics/_collector.py
  - src/yowo/cli/_main.py
  - tests/unit/test_logging.py
  - tests/unit/test_metrics_export.py
  - tests/unit/test_health_report.py
autonomous: true
requirements:
  - RELY-03
  - RELY-04
  - RELY-05

must_haves:
  truths:
    - "engine.health_report() returns a HealthReport dataclass with all required fields"
    - "yowo health CLI exits 0=ready, 1=degraded, 2=unhealthy and prints JSON"
    - "JsonFormatter emits valid JSON lines with timestamp, level, event and optional context fields"
    - "configure_logging() does not add duplicate handlers on repeated calls"
    - "engine.export_metrics() returns a JSON-serialisable dict matching EngineMetrics fields"
    - "engine.export_metrics_prometheus() returns a valid Prometheus text exposition string ending with newline"
    - "yowo metrics --format json|prometheus CLI subcommand prints the respective format"
  artifacts:
    - path: "src/yowo/logging.py"
      provides: "JsonFormatter(logging.Formatter) subclass, configure_logging() function"
      exports: ["JsonFormatter", "configure_logging"]
    - path: "src/yowo/engine.py"
      provides: "HealthReport dataclass, engine.health_report() method, export_metrics() and export_metrics_prometheus() methods, logging wired via configure_logging in load()"
      contains: "health_report"
    - path: "src/yowo/metrics/_collector.py"
      provides: "MetricsCollector.export_prometheus() method"
      contains: "export_prometheus"
    - path: "src/yowo/cli/_main.py"
      provides: "yowo health and yowo metrics CLI subcommands"
      contains: "health_command"
    - path: "tests/unit/test_logging.py"
      provides: "JsonFormatter and configure_logging unit tests"
      contains: "test_json_formatter_valid_json"
    - path: "tests/unit/test_metrics_export.py"
      provides: "Prometheus format and JSON export unit tests"
      contains: "test_prometheus_format_ends_with_newline"
    - path: "tests/unit/test_health_report.py"
      provides: "HealthReport fields and CLI exit code tests"
      contains: "test_health_report_fields"
  key_links:
    - from: "src/yowo/logging.py configure_logging()"
      to: "src/yowo/engine.py load()"
      via: "called in _finalize_load with cfg.log_level and cfg.structured_logging (fields added in plan 02)"
      pattern: "configure_logging"
    - from: "engine.health_report()"
      to: "HealthReport.memory_pct"
      via: "reads torch.cuda.memory_reserved if _is_cuda; returns None for CPU"
      pattern: "memory_pct"
    - from: "MetricsCollector.export_prometheus()"
      to: "engine.export_metrics_prometheus()"
      via: "engine delegates to self._metrics.export_prometheus()"
      pattern: "export_prometheus"
---

<objective>
Add three observability surfaces to the engine: HealthReport dataclass with `health_report()` method and `yowo health` CLI, JsonFormatter structured logging with `configure_logging()`, and Prometheus/JSON metrics export with `yowo metrics` CLI.

Purpose: RELY-03, RELY-04, RELY-05 — operators must be able to query engine health, emit structured logs for log aggregation, and scrape metrics in Prometheus or JSON format.

Output: New src/yowo/logging.py, extended engine.py and metrics/_collector.py, two new CLI subcommands, three new test files.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-reliability-and-multi-stream-scaling/02-CONTEXT.md
@.planning/phases/02-reliability-and-multi-stream-scaling/02-RESEARCH.md
@.planning/phases/02-reliability-and-multi-stream-scaling/02-01-SUMMARY.md
@.planning/phases/02-reliability-and-multi-stream-scaling/02-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts from prior plans and codebase. Executor uses these directly. -->

From src/yowo/engine.py (post plan-02) — relevant fields available in health_report():
```python
self._batch_size: int          # current batch size (may have been halved by OOM recovery)
self._original_batch_size: int # original batch size (added in plan 02)
self._oom_recovering: bool     # True when OOM recovery is active (added in plan 02)
self._selection.precision      # Precision enum; .value -> str
self._metrics.snapshot()       # -> EngineMetrics
self._is_cuda                  # property -> bool (added in plan 02)
self._health_state             # HealthStatus
self.health                    # property -> HealthStatus (derived)
```

From src/yowo/metrics/_collector.py — EngineMetrics dataclass fields:
```python
@dataclass(frozen=True)
class EngineMetrics:
    frames_total: int
    errors_total: int
    inference_mean_ms: float
    inference_p50_ms: float
    inference_p95_ms: float
    inference_p99_ms: float
    fps: float
    uptime_s: float
```

From src/yowo/config.py (post plan-02) — new InferenceConfig fields:
```python
log_level: str = "WARNING"
structured_logging: bool = False
```

From src/yowo/types.py — HealthStatus:
```python
class HealthStatus(enum.StrEnum):
    STARTING = "starting"
    READY = "ready"
    DEGRADED = "degraded"
    UNHEALTHY = "unhealthy"
    SHUTTING_DOWN = "shutting_down"
    CLOSED = "closed"
```

From src/yowo/cli/_main.py — existing CLI pattern (Click):
```python
@cli.command("benchmark")
@click.argument("source", ...)
@click.option("--model", ...)
def benchmark_command(...) -> None:
    ...
    sys.exit(0)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: JsonFormatter, HealthReport, and metrics export</name>
  <files>src/yowo/logging.py, src/yowo/engine.py, src/yowo/metrics/_collector.py, tests/unit/test_logging.py, tests/unit/test_metrics_export.py, tests/unit/test_health_report.py</files>
  <behavior>
    JsonFormatter:
    - Subclasses logging.Formatter; format(record) returns JSON string
    - Required fields: timestamp (ISO8601 UTC, e.g. "2026-03-07T10:00:00.000Z"), level (record.levelname), event (record.getMessage())
    - Optional fields emitted only if present on record: engine_id, model, backend, stream_id, frame_index, latency_ms
    - configure_logging(level="INFO", structured=False): clears existing "yowo" logger handlers before adding new one (prevents duplicates on repeated calls); sets log level
    - HealthReport frozen dataclass (slots=True): status (HealthStatus), uptime_s (float), errors_total (int), frames_total (int), memory_pct (float | None), stream_count (int), batch_size_current (int), precision_current (str)
    - HealthReport.as_dict() returns JSON-serialisable dict with status as .value string
    - engine.health_report() builds HealthReport from live metrics snapshot + OOM state; memory_pct = torch.cuda.memory_reserved / total_memory if _is_cuda else None (guarded by try/import)
    - engine.export_metrics() returns metrics.snapshot().__dict__-style dict (use dataclasses.asdict)
    - MetricsCollector.export_prometheus() returns Prometheus text format string (seven metrics: frames_total, errors_total, inference_mean_ms, inference_p95_ms, fps, uptime_seconds, memory_utilization); always ends with "\n"
    - engine.export_metrics_prometheus() delegates to self._metrics.export_prometheus()
    - configure_logging called in _finalize_load after self._loaded = True, using self._config log_level and structured_logging if config is available; use a best-effort approach (try to read config fields, fall back to WARNING/False)

    Tests:
    - test_json_formatter_valid_json: emit log record, parse output as json.loads, assert required keys present
    - test_configure_logging_no_duplicate_handlers: call configure_logging twice, assert len(logger.handlers) == 1
    - test_prometheus_format_ends_with_newline: build MetricsCollector, call export_prometheus(), assert output ends with "\n"
    - test_prometheus_metric_names: assert "yowo_frames_total" and "# TYPE yowo_frames_total counter" in output
    - test_export_metrics_dict: call engine.export_metrics(), assert all EngineMetrics field names present as keys
    - test_health_report_fields: call engine.health_report() on loaded engine with mock backend, assert status is HealthStatus.READY, assert batch_size_current >= 1
  </behavior>
  <action>
    Create src/yowo/logging.py (new file):
    - Import: json, logging, datetime; no third-party deps
    - Implement JsonFormatter and configure_logging() per CONTEXT.md pattern exactly
    - Export __all__ = ["JsonFormatter", "configure_logging"]

    In src/yowo/engine.py:
    1. Add HealthReport frozen dataclass near top of file (after imports, before BaseEngine class). Export in __all__ if __all__ exists.
    2. Add health_report() method to BaseEngine. For memory_pct: use try/except ImportError + try/except Exception to safely read torch.cuda stats; return None on any failure.
    3. Add export_metrics() method: return dataclasses.asdict(self.metrics).
    4. Add export_metrics_prometheus() method: return self._metrics.export_prometheus().
    5. In _finalize_load, call configure_logging from yowo.logging after self._loaded = True. Use getattr to safely read log_level/structured_logging from config if engine was built with InferenceConfig (config not always available in BaseEngine; add self._config optional attribute set in DetectionEngine/ClassificationEngine constructors, or read from the constructor params that were passed).

    In src/yowo/metrics/_collector.py:
    - Add export_prometheus(self) -> str method to MetricsCollector. Use snapshot() to get current values. Build lines list per RESEARCH.md Pattern 5. Return "\n".join(lines) + "\n". Include all seven metrics with # HELP and # TYPE headers.

    For HealthReport: define in engine.py as a frozen dataclass (not in types.py, to avoid circular imports with HealthStatus already in types.py). Import HealthStatus from types.py as already done.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_logging.py tests/unit/test_metrics_export.py tests/unit/test_health_report.py -x -q</automated>
  </verify>
  <done>JsonFormatter emits valid JSON with required fields; configure_logging idempotent; HealthReport returned by health_report() with all fields; export_metrics() returns serialisable dict; export_metrics_prometheus() returns valid Prometheus text ending with newline; all three new test files green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: yowo health and yowo metrics CLI subcommands</name>
  <files>src/yowo/cli/_main.py, tests/unit/test_health_report.py, tests/unit/test_metrics_export.py</files>
  <behavior>
    - yowo health CLI: no arguments; prints JSON representation of a closed-engine report; exit code 2 (unhealthy) when called without a running engine. JSON: {"status": "closed", "message": "No running engine — use engine.health_report() programmatically"}. For integration use: users call engine.health_report().as_dict() directly.
    - yowo health --format json|text option: default json; text mode prints human-readable key: value lines
    - yowo metrics CLI: --format option (json|prometheus), default json. Without running engine, emits empty snapshot (frames_total=0, all zeros). Exit code 0 always for metrics (metrics endpoint should not fail).
    - Both subcommands are registered on the cli group in _main.py
    - Import sys at top of _main.py if not already imported (use sys.exit())

    Extended tests:
    - test_health_cli_exit_code_closed: invoke `yowo health` via click.testing.CliRunner; assert exit code == 2, assert "closed" in output
    - test_metrics_cli_json_format: invoke `yowo metrics --format json` via CliRunner; assert exit code == 0, assert output is valid JSON
    - test_metrics_cli_prometheus_format: invoke `yowo metrics --format prometheus` via CliRunner; assert exit code == 0, assert "yowo_frames_total" in output
  </behavior>
  <action>
    In src/yowo/cli/_main.py:
    1. Import json at top if not present. Import sys if not present.
    2. Add @cli.command("health") with @click.option("--format", "output_format", type=click.Choice(["json", "text"]), default="json"). Body: build closed report dict, print JSON or text, sys.exit(2).
    3. Add @cli.command("metrics") with @click.option("--format", "output_format", type=click.Choice(["json", "prometheus"]), default="json"). Body: build a zero-value MetricsCollector snapshot without a running engine; for prometheus format call export_prometheus() on the collector; for json, call json.dumps with indent=2. Exit code 0.

    For MetricsCollector in metrics command: instantiate MetricsCollector(enabled=True) — it starts with all zeros — then call snapshot() for json or export_prometheus() for prometheus. Import MetricsCollector from yowo.metrics._collector.

    Add tests to test_health_report.py and test_metrics_export.py using click.testing.CliRunner. Import cli from yowo.cli._main.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_health_report.py tests/unit/test_metrics_export.py -x -q -k "cli"</automated>
  </verify>
  <done>yowo health exits 2 with JSON "closed" status; yowo metrics --format json outputs valid JSON; yowo metrics --format prometheus outputs Prometheus text with yowo_frames_total; all CLI tests green; full unit suite clean: uv run pytest tests/unit/ -x -q</done>
</task>

</tasks>

<verification>
- `uv run ruff check src/yowo/logging.py src/yowo/engine.py src/yowo/metrics/_collector.py src/yowo/cli/_main.py --quiet` — no lint errors
- `uv run pyright src/yowo/logging.py src/yowo/engine.py src/yowo/metrics/_collector.py` — no type errors
- `uv run pytest tests/unit/test_logging.py tests/unit/test_metrics_export.py tests/unit/test_health_report.py -x -q` — all three new test files green
- `python -c "import json; import subprocess; r = subprocess.run(['uv','run','yowo','health'], capture_output=True, text=True); d = json.loads(r.stdout); assert d['status'] == 'closed'; print('health CLI ok')"` — manual smoke test
- `uv run pytest tests/unit/ -x -q` — full unit suite clean
</verification>

<success_criteria>
- src/yowo/logging.py exists with JsonFormatter and configure_logging() using stdlib only
- engine.health_report() returns HealthReport with status, uptime_s, errors_total, frames_total, memory_pct (None on CPU), stream_count, batch_size_current, precision_current
- engine.export_metrics() returns JSON-serialisable dict; engine.export_metrics_prometheus() returns Prometheus text ending with newline
- yowo health CLI exits 0=ready / 1=degraded / 2=unhealthy/closed and prints JSON
- yowo metrics --format json|prometheus CLI exits 0 and prints correct format
- configure_logging() called twice does not add duplicate handlers
- All three new test files green; existing test_health.py, test_metrics.py, test_cli.py remain green
</success_criteria>

<output>
After completion, create `.planning/phases/02-reliability-and-multi-stream-scaling/02-03-SUMMARY.md`
</output>
