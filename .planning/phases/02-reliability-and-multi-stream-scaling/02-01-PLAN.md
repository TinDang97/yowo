---
phase: 02-reliability-and-multi-stream-scaling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/yowo/pipeline/_collector.py
  - src/yowo/types.py
  - tests/unit/test_collector.py
autonomous: true
requirements:
  - STRM-01
  - STRM-02
  - STRM-03
  - STRM-04
  - STRM-05

must_haves:
  truths:
    - "A stream that hits 3 consecutive read errors is automatically removed without affecting other streams"
    - "Per-stream stats (frames_processed, consecutive_errors, last_frame_time) are tracked in _StreamEntry"
    - "remove_stream() drains the queue and joins the bridge thread before clearing references — no tracemalloc leaks"
    - "Frame drop rate per stream is measurable via frames_dropped counter"
    - "StreamConfig dataclass exposes auto_reconnect with exponential backoff (1s→2s→4s, max 30s)"
  artifacts:
    - path: "src/yowo/pipeline/_collector.py"
      provides: "Extended _StreamEntry with per-stream stats; auto-remove on 3 consecutive errors via sentinel mechanism; _stop_entry drains and joins"
      contains: "consecutive_errors"
    - path: "src/yowo/types.py"
      provides: "StreamConfig dataclass"
      exports: ["StreamConfig"]
    - path: "tests/unit/test_collector.py"
      provides: "Tests for auto-remove, memory-leak (tracemalloc), frame drop stats, stream isolation"
      contains: "test_remove_stream_no_leak"
  key_links:
    - from: "_run_bridge (bridge thread)"
      to: "FrameCollector auto-remove"
      via: "sentinel flag on _StreamEntry, not direct remove_stream() call — avoids self-join deadlock"
      pattern: "entry\\.auto_remove"
    - from: "_stop_entry"
      to: "bridge thread"
      via: "stop_event.set() + bridge.join(timeout=5.0)"
      pattern: "bridge\\.join"
---

<objective>
Extend FrameCollector and _StreamEntry with production-grade per-stream isolation: automatic stream removal on repeated failures, per-stream stats tracking, optional auto-reconnect, and verified memory-clean removal.

Purpose: STRM-01 to STRM-05 — multi-stream pipeline must handle individual stream disconnection without affecting others, track per-stream frame drop stats, and leave no memory leaks when streams are removed.

Output: Extended pipeline/_collector.py, new StreamConfig in types.py, extended tests/unit/test_collector.py.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-reliability-and-multi-stream-scaling/02-CONTEXT.md
@.planning/phases/02-reliability-and-multi-stream-scaling/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts extracted from codebase. Executor uses these directly. -->

From src/yowo/pipeline/_collector.py (current _StreamEntry — extend, do not replace):
```python
class _StreamEntry:
    __slots__ = ("bridge", "error", "reader", "source", "state", "stop_event")

    def __init__(self, reader: ThreadedFrameReader, source: FrameSource) -> None:
        self.reader: ThreadedFrameReader = reader
        self.source: FrameSource = source
        self.state: StreamState = StreamState.RUNNING
        self.error: BaseException | None = None
        self.bridge: threading.Thread | None = None
        self.stop_event: threading.Event = threading.Event()
```

Current _run_bridge signature:
```python
def _run_bridge(stream_id: str, entry: _StreamEntry, shared_q: queue.Queue[...]) -> None
```

Current _stop_entry:
```python
def _stop_entry(entry: _StreamEntry) -> None:
    # sets stop_event, calls reader.stop(), joins bridge with timeout=5.0
```

Current remove_stream():
```python
def remove_stream(self, stream_id: str) -> None:
    # acquires self._lock, pops entry, calls _stop_entry(entry)
```

From src/yowo/types.py (add StreamConfig here alongside HealthStatus):
```python
class HealthStatus(enum.StrEnum):
    STARTING = "starting"
    READY = "ready"
    DEGRADED = "degraded"
    UNHEALTHY = "unhealthy"
    SHUTTING_DOWN = "shutting_down"
    CLOSED = "closed"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StreamConfig dataclass and extended _StreamEntry</name>
  <files>src/yowo/types.py, src/yowo/pipeline/_collector.py</files>
  <behavior>
    - StreamConfig(auto_reconnect=False, max_consecutive_errors=3, reconnect_backoff_base_s=1.0, reconnect_backoff_max_s=30.0) — frozen dataclass with slots
    - _StreamEntry gains new __slots__: "auto_remove", "consecutive_errors", "frames_dropped", "frames_processed", "last_frame_time" — all initialised to 0/0.0/False
    - Bridge thread increments consecutive_errors on exception; resets to 0 on successful Frame read
    - When consecutive_errors reaches entry's threshold (from StreamConfig.max_consecutive_errors), bridge sets entry.auto_remove = True and breaks — does NOT call remove_stream() directly (avoids self-join deadlock per RESEARCH.md pitfall 2)
    - frames_processed increments each time a Frame is successfully put into shared_q
    - frames_dropped increments each time _put_or_stop returns False (frame discarded due to full queue or stop)
    - last_frame_time updated to time.monotonic() on each successful Frame read
    - FrameCollector.__iter__ checks for (stream_id, None) sentinel with auto_remove=True and calls remove_stream() from the iterator thread (not bridge thread) — safe because iterator is distinct from all bridge threads
    - StreamConfig exported from src/yowo/types.py and from src/yowo/pipeline/__init__.py
  </behavior>
  <action>
    In src/yowo/types.py: Add StreamConfig as a frozen dataclass with slots=True after HealthStatus. Export it in __all__.

    In src/yowo/pipeline/_collector.py:
    1. Extend _StreamEntry.__slots__ with: "auto_remove", "consecutive_errors", "frames_dropped", "frames_processed", "last_frame_time". Update __init__ to initialise all new fields.
    2. Update _run_bridge: on successful Frame read, reset entry.consecutive_errors = 0, increment entry.frames_processed, update entry.last_frame_time. On exception, increment entry.consecutive_errors; if >= max_consecutive_errors (pass this value into _run_bridge as a parameter), set entry.auto_remove = True, log structured warning with stream_id, break. On _put_or_stop returning False, increment entry.frames_dropped.
    3. Update FrameCollector.add_stream() to accept optional stream_config: StreamConfig | None = None parameter. Store max_consecutive_errors from it in _run_bridge call. Default to StreamConfig() if None.
    4. In FrameCollector.__iter__, after receiving (stream_id, None) sentinel: if entry.auto_remove is True, call self.remove_stream(stream_id) from within the iterator (safe — iterator thread, not bridge thread). Log structured warning: "Stream {stream_id} auto-removed after {N} consecutive errors".
    5. _stop_entry must drain the queue of any pending items for this stream before joining. After stop_event.set() and reader.stop(), drain with a short loop: call bridge.join(timeout=5.0). No change to join timeout.

    NOTE: Do NOT add auto-reconnect loop in this task — auto-reconnect is an optional future extension. StreamConfig fields are defined but reconnect loop is not implemented in this plan (reconnect backoff fields stubbed for future use, max_consecutive_errors and auto_remove are the active fields).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_collector.py -x -q -k "consecutive or auto_remove or frames_processed or frames_dropped"</automated>
  </verify>
  <done>_StreamEntry tracks per-stream stats; bridge auto-sets auto_remove on threshold; iterator calls remove_stream safely; StreamConfig exported from types.py</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Memory-leak and stream isolation tests</name>
  <files>tests/unit/test_collector.py</files>
  <behavior>
    - test_remove_stream_no_leak: add stream, process frames, remove stream, explicit gc.collect(), take tracemalloc snapshot — memory delta < 500KB
    - test_stream_isolation: two streams; one stream hits 3 errors (mock reader raises); only that stream is removed; second stream continues yielding frames unaffected
    - test_frame_drop_stats: frames_dropped increments correctly when queue is full (use max_queue_size=1 to force drops)
    - test_frames_processed_counter: frames_processed matches frames successfully enqueued
    - test_auto_remove_on_consecutive_errors: bridge sets auto_remove after max_consecutive_errors threshold; FrameCollector.__iter__ calls remove_stream; stream_id no longer in collector._streams
    - All tests use mock readers (no real RTSP); test file is self-contained
  </behavior>
  <action>
    Extend tests/unit/test_collector.py with the new test functions. Existing tests must remain green. Use tracemalloc.start() + gc.collect() pattern for the leak test. Use threading.Event() to synchronise stream exhaustion in isolation test.

    For test_remove_stream_no_leak:
      1. tracemalloc.start()
      2. Create collector, add stream, iterate 10 frames, call remove_stream()
      3. gc.collect() × 2
      4. snapshot = tracemalloc.take_snapshot(); tracemalloc.stop()
      5. Assert peak size delta from tracemalloc statistics is < 500_000 bytes

    For stream isolation: mock reader A raises Exception after frame 1; mock reader B yields 5 frames cleanly. Assert reader B's 5 frames are all received. Assert stream A's stream_id is not in collector._streams after auto-remove.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_collector.py -x -q</automated>
  </verify>
  <done>All new and existing collector tests green; tracemalloc leak test passes; stream isolation test proves independent failure; full unit suite clean: uv run pytest tests/unit/ -x -q</done>
</task>

</tasks>

<verification>
- `uv run ruff check src/yowo/pipeline/_collector.py src/yowo/types.py --quiet` — no lint errors
- `uv run pyright src/yowo/pipeline/ src/yowo/types.py` — no type errors
- `uv run pytest tests/unit/test_collector.py -x -q` — all collector tests green including new ones
- `uv run pytest tests/unit/ -x -q` — full unit suite clean
</verification>

<success_criteria>
- FrameCollector handles stream auto-removal on 3 consecutive errors without deadlock or memory leak
- Per-stream stats (frames_processed, frames_dropped, consecutive_errors, last_frame_time) tracked in _StreamEntry
- StreamConfig dataclass exported from types.py with auto_reconnect, max_consecutive_errors, reconnect_backoff_base_s, reconnect_backoff_max_s fields
- Tracemalloc leak test passes (< 500KB delta after remove_stream + gc)
- Stream isolation test proves one stream's failure does not affect other streams
- All existing collector tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/02-reliability-and-multi-stream-scaling/02-01-SUMMARY.md`
</output>
