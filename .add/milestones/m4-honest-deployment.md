---
type: Milestone
title: Exported and quantized artifacts do what their metadata says
status: direction
generated: { by: add/3.5.0, at: 2026-09-08 }
verified: []
advised_by: milestone-planner
---
## CARD
goal: An exported or quantized artifact does exactly what its metadata says, and its cost is measurable before deployment.
why: `precision` is accepted by config and reported by `health_report()` while being consumed by zero backends. ONNX export writes a graph whose own sidecar cannot describe it and which fails to load on a default install. INT8 is calibrated on stretch-resized images against letterboxed inference, under a docstring claiming they match — with no accuracy gate anywhere. Caches are keyed on filenames. An operator currently cannot answer what latency or accuracy they will get before shipping.
next: add new task <slug>

## SCOPE
In:  ONNX external-data and opset truth · precision plumbed to backends or removed from the surface · TensorRT engine and INT8 calibration cache keying · INT8 calibration/inference preprocessing parity and a measured accuracy delta · bounded postprocess (pre-NMS top-k, max_det) · benchmark backend attribution · feature-cache honesty · the `ExportResult`/`ExportMetadata` contract
Out: the public API stability policy around these types (→ m5-declare-ga) · new backends or new model families

## GROUND
touches: src/yowo/export/ · src/yowo/backends/ · src/yowo/postprocess/ · src/yowo/benchmark/ · src/yowo/tune/ · src/yowo/cache/ · src/yowo/config.py · src/yowo/engine.py
risks:
  - Plumbing `precision` for real will change output numerics on backends that silently ran FP32 while reporting FP16. Users' current numbers are the wrong ones, but they are the ones they have.
  - Fixing INT8 calibration to letterbox changes every existing quantized artifact's scales. Old `.calib` caches must be invalidated, not reused.
  - Adding `max_det` bounds p99 by discarding detections in dense scenes — a behaviour change that needs a default chosen deliberately and documented.

## EXIT
- [ ] Every exported artifact loads from a clean environment using only what its sidecar names, **and the sidecar names every file the export produced, with opset, input shape and dynamism read back from the produced artifact**   (← onnx-external-data) · AMENDED 2026-09-13: the original wording was satisfiable by a sidecar listing two files while still lying. Measured — (a) the "recorded opset" clause is VACUOUS: `ExportMetadata` has no opset field, so nothing can disagree; requested opset 17 silently produces 18 on every export. (b) With onnxslim the `.onnx` IS self-contained (external 0/224, clean-room load PASS) but leaves a 9,764,864 B orphan `.onnx.data` named by nothing; WITHOUT onnxslim (the documented `yowo[pytorch,onnx]` install) the `.onnx` is a 774,054 B stub needing 147 external initializers and the sidecar records `file_size_bytes: 774054`. So the box is false INSTALL-CONDITIONALLY. (c) `yolo11n-cls` sidecar says `input_shape=[1,3,640,640]` for a graph that is `[1,3,224,224]` — an 8.2x pixel-count lie. (d) `ExportMetadata.load()` has zero callers in `src/`, so the sidecar is write-only and none of this is caught at load. RESIDUAL: the FP16-default break that blocked this box was fixed separately by `export-fp16-default`.
  RE-VERIFIED BY MEASUREMENT 2026-09-16, before authoring the node. All four findings still hold; three of the recorded NUMBERS do not, and today's are used instead. yolo11n / fp32 / dynamic_batch, `uv run` on darwin-arm64.
  (a) CONFIRMED and sharper: `torch.onnx.export(opset_version=17)` produces `ai.onnx 18`. Torch says so on the way past — "Setting ONNX exporter to use operator set version 18 ... Automatic version conversion will be performed" — then the downconversion RAISES (`No Adapter To Version $17 for Resize` on detect, `axes_input_to_attribute ... No initializer or constant input` on classify) and the failure is swallowed: the export completes at 18 and returns success. `ExportMetadata` has no `opset` field, so nothing records the 17 that was asked for or the 18 that was produced. The clause is vacuous because the field does not exist, not because the values agree.
  (b) CONFIRMED, both limbs, with today's sizes. WITH onnxslim the `.onnx` is 10,641,583 B and self-contained (external initializers 0/194, clean-room load PASS) — and a 10,616,832 B `.onnx.data` is left in the directory, named by nothing, exactly doubling the artifact on disk (real total 21,258,415 B against a sidecar `file_size_bytes` of 10,641,583 — 2.00x). WITHOUT onnxslim the `.onnx` is a 612,255 B STUB needing 130 of 201 initializers from that same file, the sidecar records `file_size_bytes: 612255` against a real 11,229,087 B (18.34x), and the clean-room load FAILS: `ValidationError: Data of TensorProto (tensor name: backbone.stem.conv.weight) should be stored in .../yolo11n.onnx.data`. The recorded 9,764,864 B / 774,054 B / 147-initializer figures are from 2026-09-13 and are superseded.
  WHICH INSTALL IS THE BROKEN ONE, corrected: the box says `yowo[pytorch,onnx]`. `onnxslim` IS pinned in the `export` extra and in the dev group, so `yowo[export]` and CI both have it. The reachable break is `yowo[pytorch]` — and that is the install `_exporter.py:94` ITSELF NAMES (`DependencyError("torch", "uv add yowo[pytorch]")`), because ONNX export guards on `torch` alone and reaches `onnx`/`onnxslim` only inside a `try: ... except ImportError: logger.debug(...)`. So the documented remedy for the only dependency the export checks is the environment that produces the unloadable artifact, quietly, at debug level. Because `onnxslim` is in the dev group, NO CI JOB EXERCISES THAT PATH.
  (c) CONFIRMED: `yolo11n-cls` exports a graph whose input is `['batch', 3, 224, 224]` while the sidecar says `input_shape: [1, 3, 640, 640]` and `imgsz: 640` — 8.16x the pixels. `_exporter.py:228` writes `[1, 3, imgsz, imgsz]` from the REQUEST; nothing reads the produced graph.
  (d) CONFIRMED: `ExportMetadata.load()` has zero callers in `src/` — the sidecar is write-only, so none of (a)–(c) is caught at load.
  (e) NEW, FOUND WHILE RE-VERIFYING, and not in this box's wording: `model_stem = f"{family}{size}{task_suffix}"` at `_exporter.py:151-153` gives `task_suffix` a branch for `obb` and NONE for `classify`. A classify export of yolo11n therefore writes `yolo11n.onnx` with `model_name: yolo11n` — byte-for-byte the same path and the same recorded identity as the detect export, and the sidecar has no `task` field to tell them apart. Two exports into one output directory silently overwrite, and the survivor's sidecar describes whichever ran last. Measured: the detect and classify probes both produced `yolo11n.onnx` + `yolo11n.yowo.json`.
  LANDED 2026-09-16 (PR #70 and PR #71) and STILL NOT TICKED. The box says "**Every** exported artifact"; what is now true is true of ONNX. It is false of tensorrt, openvino and coreml, and ticking would claim them.
  WHAT ONNX NOW DOES, measured on this machine before and after. `_export_onnx` internalizes external tensor data and removes the companion, which `_export_onnx_kv:353-360` has always done for the CoreML EP while the ordinary path did not — so onnxslim was load-bearing by accident. With onnxslim: 10,641,583 B self-contained and NO orphan, against 21,258,415 B on disk before. Without it: 11,160,405 B self-contained and a clean-room load that PASSES, against a 612,255 B stub whose clean-room load raised `ValidationError: Data of TensorProto (backbone.stem.conv.weight) should be stored in .../yolo11n.onnx.data`. `parity.yml` now runs that no-onnxslim leg on both entry points, and the job log confirms 9 tests actually executed on Linux rather than a step that collected nothing.
  `opset` records 18 — what torch produces — and keeps `requested_opset: 17` to say the ask was refused. `input_shape` and `imgsz` come off the graph, so `yolo11n-cls` records 224 where it recorded 640. `artifact_files` names every file the export added, entry first; `total_size_bytes` sums them while `file_size_bytes` keeps meaning the entry alone. `model_stem` suffixes classify `-cls`, so finding (e) is gone. `ExportMetadata.load()` has its first caller in `src/`: `export_model` reads its own sidecar back and refuses one that does not describe the directory — and that guard runs for EVERY format, so the box's "names every file the export produced" clause does hold for all five.
  DYNAMISM, AND HOW IT WAS NEARLY MISSED. The box names three things read back — opset, input shape and DYNAMISM — and PR #70 delivered two. `dynamic` was still the request. It was caught by reading the box's literal words before ticking it, NOT by a check, and no check could have caught it as first written: a check that exported with `dynamic_batch` True and False and compared the sidecar to the graph PASSED GREEN against the un-fixed exporter, because on this torch `dynamic_axes` is honoured exactly and a copy is indistinguishable from a read-back. PR #71 adds the leg that forces the divergence — `dynamic_axes` dropped on the way to torch, so `dynamic_batch=True` yields a static graph — which is red before and green after. This is Q15 in a new dress: a check must be exercised where it CAN fail, not only where it happens to agree.
  RESIDUAL, and the reason this box stays open: for tensorrt, openvino and coreml the sidecar still carries the REQUEST for shape, opset and dynamism, because `read_onnx_graph_facts` cannot read an engine, a converted directory or a package and returns unknowns. The file-set clause holds for them; the read-back clause does not. Worse, nothing PROVES the load clause for them either — no test in any tier runs a real tensorrt or openvino export, because neither package is in the dev group, so A11 and A12 are bound at `produced_files` and `read_onnx_graph_facts` where the decisions are made rather than end to end, and both docstrings say so. Closing this box needs either a converted-format readback or a decision that "every exported artifact" means the ONNX one the box's own text is about — the second is a human call, not mine to take silently.
  Node gated PASS at authority human, 17 checks bound, after TWO refusals worth recording. The first named five unbound rules: two were an engine-grammar artifact (`R:[A-Z0-9_]+` at `add.py:4097`, so `R:REQUEST-AS-FACT` parsed as the id `R:REQUEST` and never matched its `covers:` token) and three — A6, A11, A12 — were a real gap, bound by new checks rather than waived. The node was then REOPENED to direction for M9/E7 rather than amended after the fact, because a new rule belongs in Direction.
  THE MUTATION SWEEP FOUND TWO OF MY OWN CHECKS UNFAILABLE, which is the same defect class this box is about. `test_the_recorded_size_accounts_for_every_produced_file` compared the total against the directory sum — but after internalization the export leaves ONE file, so total and entry are the same number and reverting the field passed; it now lowers the protobuf ceiling so the companion survives and asserts the two DIFFER. `test_export_sidecar_truth_runs_in_ci` accepted `exact_module in c or "tests/integration" in c`, and the second clause made it unfailable. The sweep harness itself was wrong twice: a bad nodeid collected zero tests and the classifier read "no tests ran" as green, making 13 of 14 rows meaningless; and its `git checkout -- .` silently discarded an uncommitted test fix and then reported the stale check. Both filed as lessons.
  THREE DECISIONS WENT TO THE USER AND TIMED OUT — record the produced opset rather than raise, add the `-cls` suffix, internalize rather than require `yowo[export]`. `interviews/1.md` records them as MINE with what was asked and what was taken in the silence. The freeze stamp reads `human` because the engine computes that floor from `sensitive_paths:`, not because a human chose these three. The classify path move is user-visible and is the one to raise.
- [x] `precision` reaches the backend that executes, or no longer appears in config, health, **CLI, tune profiles or benchmark rows**. Plumb-vs-remove is a human decision — the two differ in reversibility, not effort   (← precision-plumbing) · AMENDED 2026-09-13: `precision` is not in `EngineMetrics` at all, so the original enumeration named a surface it does not occupy and missed the two where the inert value does damage — `TuneProfile.precision` (auto-applied to production configs at `engine.py:169-187`) and `SweepResult.precision`. Measured: `create_backend()` has NO `precision` parameter (`backends/__init__.py:142-199`); `PyTorchBackend(fp16=...)` is the only hook and is never passed. Asymmetry: `fp16` is silently coerced to fp32 by the CPU branch, but `int8` is passed through and REPORTED while fp32 executes. DECIDED 2026-09-13 — PLUMB WHERE REAL, RAISE WHERE NOT (not remove, not plumb-everywhere): honour `precision` on backends that can implement it; RAISE loudly when a backend cannot honour an EXPLICITLY requested precision rather than silently coercing; and drop it from surfaces that only report it, including `TuneProfile`'s dead sweep axis. Silent coercion is the actual defect — `int8` today reports one value and executes another. Note the knob is backend- AND device-conditional: `PyTorchBackend`'s fp16 hook autocasts only under CUDA (`_pytorch.py:354-355`), so 'cannot honour' is the common case and must be loud. RESIDUAL: `_try_precision_fallback` (`engine.py:723-736`) is dead — no backend defines `set_precision`, so OOM tier-2 counts a degradation for a recovery that never happened.
  LANDED 2026-09-15 (PR #52), TICKED on the FIRST limb — precision now reaches the backend that executes. `create_backend()` takes `precision` and `precision_explicit` and threads both to all five backends; `InferenceBackend` gained `executing_precision`; PyTorch resolves inside `load()` after the device does. The second limb is NOT claimed: `TuneProfile.precision` still exists, because removing a field from a public frozen dataclass is m5's `deprecation-policy` call. What dropped is the sweep's ENUMERATION over it plus `_load_tune_profile`'s write of it into a production config, so a non-`None` `config.precision` now means exactly one thing: a human asked for it.
  ONE COURSE CORRECTION, caught by CI and worth recording. The first build read "a backend honours P only if its own runtime compute uses P", which turned "I cannot determine what I execute" into "I do not execute this" — so an FP32-exported ONNX artifact answering an explicit `fp32` request HARD-FAILED, and Backend Conformance went red on 8 tests while the unit suite was 2656 green. M2 had always said "a request that is not what the loaded backend ACTUALLY EXECUTES", a demonstrated mismatch; the assumption beneath it over-read the rule. The verdict is now three-way — executes P (silent) · executes something else, SHOWN (raise) · cannot determine (accept, and report `"unknown"`). Determinability was measured, not assumed: a half boundary is conclusive (an FP16 export declares `tensor(float16)` at both ends, so it refuses an explicit fp32), a float boundary is not (an INT8 QDQ graph built from FP32 is `tensor(float)` at both ends, identical to FP32, and a file size is not a precision oracle).
  THREE DEFECTS FIXED THAT THE BOX DID NOT NAME: (a) `_apply_oom_recovery` RETURNED after the dead tier-2 call, so `_halve_batch_size` — the only working recovery — was UNREACHABLE in `[0.90, 0.95)`; the phantom rung swallowed the real one at the highest pressure below eviction. (b) `_try_precision_fallback` set DEGRADED without `_oom_recovering`, and the restore branch is the only thing that sets READY back, so one tick latched DEGRADED permanently. (c) All three artifact backends first raised the precision `ConfigError` INSIDE the session-creation `try`, which re-raised it as `BackendLoadError` — which the fallback loop catches — so a wrongly-typed `--precision` would have been answered by quietly running a different backend. That is this box's own defect reintroduced one layer down, and it was caught in build.
  RESIDUALS: the OpenVINO leg of the E11 unit check runs NOWHERE in CI (`openvino` is absent from the dev group and `ci.yml` installs it only for the `conformance` job, which runs a different directory) — the behaviour is covered there, the unit leg is not; CoreML determines nothing and always reports `"unknown"`; "asked fp16, got an FP32 export" is now quiet-and-correctly-labelled rather than loud-and-sometimes-wrong, knowingly accepted; and `metrics-truth` (gated PASS 2026-09-12) had one of its three degradation sites retired, so its M4/A16/E6 are amended in place — A16 rested on the factual error that the fallback attempted a recovery and failed, when it attempted nothing. Filed as method lesson M18.
  THE RACE, RESTATED HONESTLY: `_halve_batch_size` rebinds `_preprocess_buf` from the monitor thread while `_run_gpu` reads it under a lock the monitor never takes — measured trippable at 276 of 1683 locked reads. This node does NOT create it (it was already live at `[0.80, 0.90)`); it WIDENS the exposed band to `[0.80, 0.95)`. Unowned: it needs a threading node.
- [ ] **Each cache key names its full invalidation set — GPU arch, TRT version, driver, precision, shape profile, calibration set, model+backend+runtime version, and the cgroup-aware cpu count — and a test perturbs each element and asserts the key changes. The feature cache additionally refuses a hit on any shape change.**   (← artifact-cache-keys) · AMENDED 2026-09-13: UNSATISFIABLE as originally written — it read as a test-only task, but 6 of the 7 named elements are in NO key anywhere, so there was nothing to perturb. Grepped `src/`: `trt_version` 0 hits, `driver_version` 0, `shape_profile` 0, `calibration_set` 0, `trt_engine_cache_prefix` 0; `driver_version` is not even probed (`_detect.py:330` queries `index,name,memory.total,memory.free`). This is a BUILD task, not a test task. ALREADY TRUE: the cgroup-aware cpu count does invalidate the tune key (1→b0b22904, 2→c74a1a18, 4→767c8467, 8→f766d864, 16→57c9c0ff) — `container-aware-sizing` landed; do not re-litigate it. RESIDUAL: the tune key omits backend AND precision, so ONNX and PyTorch profiles collide at `profiles/fa3304a0/yolo11n.yaml` and the second sweep silently overwrites the first.
- [x] INT8 calibration uses the inference preprocessing path; **a quantized artifact is written only if its measured delta against its FP32 source clears a declared floor, and ONNX INT8 is repaired or withdrawn**   (← int8-calibration-parity) · AMENDED 2026-09-13: the original second clause presumed a quantized artifact worth measuring exists. It does not. Measured — the preprocessing mismatch is real (calibration stretch-resizes, inference letterboxes: mean|Δ| = 0.191968, 91.72% of pixels differ >0.01, 25.00% of the inference tensor is padding calibration never sees) — but FIXING it changes nothing. ONNX INT8 produces ZERO detections at 0.25 in all four configs tried (entropy/minmax, QInt8/QUInt8 activations, per-channel, and with letterboxed calibration): fp32 top-5 conf [0.9227 0.9159 0.9026 0.8631 0.5758] → int8 [0 0 0 0 0]. The artifact loads, infers and returns a well-formed (1,300,6) — an artifact that lies, which is worse than a crash. As originally written this box would land, measure mAP→0, and have nothing to ship. RESOLVED 2026-09-13 by bounded diagnosis — REPAIRABLE, do not withdraw. The cause is the end2end decode/topk tail: `ReduceMax -> ArgMax -> TopK -> GatherElements -> Expand -> GatherElements -> Cast -> Concat`, plus the DFL box-decode arithmetic ahead of it. QDQ destroys it, which is why boxes came back degenerate (6.62 px) rather than merely imprecise. Excluding that tail from quantization restores detections with the size win intact: 5 dets vs FP32's 5, class ids IDENTICAL [5,0,0,0,0], max box delta 2.998 px on a 640 input, max conf delta 0.1152, artifact 2,959,198 B = 30.1% of FP32. The 102 Conv layers where the size win lives still quantize. RESIDUALS for the node: (a) the exclusion set must be selected topologically from the decode split onward, not by node index — the diagnosis used a blunt last-45-nodes-by-op-type heuristic that would break silently when the graph changes; (b) this is ONE image on ONE model, so it proves the mechanism, not an accuracy delta — the real number needs the m3 dataset; (c) the weakest detection fell 0.5253 -> 0.4101, a 21% relative loss sitting near the 0.25 default threshold, so the floor should gate on RECALL against FP32 and not on box error alone.
  SUPERSEDED 2026-09-15: the cause recorded above is WRONG, and residual (a) is answered. `end2end` is YOLO26-only (`src/yowo/arch/_config.py:69,74`); a production-path `yolo11n` FP32 ONNX export has 325 nodes, 87 `Conv`, and ZERO `TopK`/`ArgMax`/`GatherElements`/`ReduceMax` — and fails identically. So the end2end tail is a YOLO26-specific SUBSET of the real culprit: the post-head decode subgraph (DFL softmax, box decode, final sigmoid). A detail that explains why the artifact looked healthy: it is not "all zeros" — the 4 box-geometry rows still decode and only the 80 class rows are exactly zero, so shape, dtype and boxes all look plausible. Two further numbers above were mis-measured by the orchestrator and are corrected here: "102 Convs" was the yolo26n count (yolo11n has 87), and the "4/5 recall" was an artifact of a calibration set of 12 identical copies of one image — a real set gives 5/5, which is Q14 again.
  LANDED 2026-09-15 (PR #53) and TICKED against the box's literal three clauses: (1) calibration uses the inference path — `calibration_batches` now calls `yowo.io._decode.preprocess`, so the two letterbox identically BY CONSTRUCTION rather than by an A/B that happened to agree. (2) written only if its measured delta clears a DECLARED floor — the write is gated: quantize to a `.partial` sibling, measure post-NMS recall against the FP32 source, `os.replace` on pass or delete and raise; `parity_floor`, `parity_margin`, `parity_threshold`, `parity_iou`, `parity_sample` are all declared parameters, not constants. (3) repaired, not withdrawn — 0 detections -> 6 against FP32's 5, at 2,984,548 B = 0.281x. The exclusion set is computed by a backward walk from every graph output terminating at `Conv`, keyed on TENSORS not node names (ONNX does not enforce unique node names), 22 nodes on yolo11n and 28 on yolo26n with no family branch.
  RESIDUALS, named not buried: (a) THE FLOOR GATES RECALL ONLY. The artifact emits 6 boxes against FP32's 5 at total_recall 1.0 — `int8_unmatched` RECORDS a false-positive flood, nothing GATES it. The 6th box is class 7 at 0.2579, just above the 0.25 default, and nobody has yet established whether it is a duplicate, an NMS artifact or a genuine weak find. A `parity_precision_floor` is the follow-up and it needs a real dataset, not one image. (b) THE PARITY SET IS THE CALIBRATION SET — `held_out: false` is stamped on the artifact. This is a COLLAPSE DETECTOR, not an accuracy certification; a held-out labelled measurement needs the m3 dataset (now landed, PR #51) and plugs into `parity_images`. (c) INT8 + `kv_cache` and INT8 for `classify`/`obb` are now REFUSED BY NAME — a user-visible change to `export_model`, taken because neither could produce a working artifact and a warn-then-emit-FP32 would force the sidecar to lie about precision.
- [ ] Postprocess **latency** is bounded independent of scene content **on both the axis-aligned and the oriented path**, with the bound measured at each path's real anchor count   (← bounded-postprocess) · AMENDED 2026-09-13: "cost" reads as memory, and memory is FINE — traced peak is strictly linear at 0.697 KB/candidate (69.7 MB at 100k), so the allocation concern is retired as measured. The unbounded quantity is TIME. Measured: detection at the real 8400-anchor count is 176 ms (disjoint) / 281 ms (random classes) against 0.15 ms at zero boxes — a ~1200x swing on scene content alone. OBB is 43x worse and was invisible to the original sizing, which was set against a review citing only `_nms.py`: `_nms_rotated` is a Python loop with an `int(order[i].item())` device sync per candidate, measured 3918 ms at 8400 anchors and 23.9 SECONDS at the real DOTA 21504 count. Also reachable with one documented flag: `--confidence 0.01` takes postprocess from 0.22 ms to 68 ms per frame forever. RESIDUAL: `cv2.dnn.NMSBoxes` already accepts `top_k` and yowo passes none — measured 67.41 ms → 0.96 ms for that one argument; the OBB path cannot be bought that way. SIZE: standard → deep.
- [ ] **Every figure a benchmark or tune row reports is measured for that row**: the backend that actually executed (not the request), the artifact actually loaded (not the source `.pt`), and an input that includes the postprocess cost the row is meant to rank   (← benchmark-truth) · AMENDED 2026-09-13: the original wording covers only one of three misattributed columns. Measured — (a) `BenchmarkResult.format` reports the REQUEST: format="onnx" while `engine.selection.backend`=pytorch after a fallback. (b) `model_size_mb` is 5.354 MB for ALL FIVE formats because `_get_model_size_mb` always stats the source `.pt`, never the loaded artifact, and returns a literal `0.0` on failure that renders as a measurement. (c) The OBB branch never passes `backend=` to `OBBEngine` at all, so the requested configuration is never EXECUTED, yet the row is labelled with it. (d) `tune` measures FPS on `np.zeros((640,640,3))` → zero detections → 0.15 ms of postprocess, excluding 100% of the 176–281 ms it is ranking, then persists the winner and auto-applies it to production configs.
  LANDED 2026-09-16 (PR #68) and DELIBERATELY NOT TICKED. The bold headline holds — no benchmark or tune row now reports a figure that was not measured for that row — but the box then ENUMERATES three figures, and only the first is delivered. Ticking would claim the other two.
  DELIVERED (limb 1): `format` is the backend that EXECUTED, read from `engine.selection` inside the `with` block exactly as `device` already was; `requested_format` keeps the request and the table prints `pytorch (asked: onnx)` so a fallback is legible where it is read. Re-verified by measurement 2026-09-16 — all four recorded findings still held. Before: requested onnx logged `Primary backend onnx failed, trying pytorch` and the row still said `format=onnx`, both rows at `model_size_mb=5.354`, 16.97 vs 20.31 fps — pytorch against pytorch, read as onnx being 17% slower. After: `onnx → executed pytorch` on its face. The OBB branch now passes `backend=` (and `num_classes=`, which no check in the node binds and which the commit says so); the no-latencies early return no longer hardcodes `device="unknown"`.
  NOT DELIVERED (limb 2), "the artifact actually loaded (not the source `.pt`)": for onnx, openvino and tensorrt rows the size is now `None` and the table prints a dash. The misattribution is gone — `model_size_mb` was 5.354 MB for every format because `_get_model_size_mb` stats the spec's checkpoint, and two failure paths returned a literal `0.0` that rendered as "0.0 MB" — but ABSENCE IS NOT MEASUREMENT. No backend exposes the path of the file it loaded: `selection` carries backend, device, precision and reason, and `PyTorchBackend` holds a spec, not a path. Reporting it needs an accessor on the backend Protocol, which is `src/yowo/backends/` — sensitive, a public contract surface, and `backend-extension-contract`'s subject. UNOWNED BY THIS BOX until that node lands.
  NOT DELIVERED (limb 3), "an input that includes the postprocess cost the row is meant to rank": the DEFAULT is still `np.zeros((640,640,3))`. `_measure_config` and `run_sweep` accept `sample_frames`, and the row and persisted `TuneProfile` now carry `postprocess_representative` so the measurement SAYS it is not representative — but a caller who passes nothing still ranks on a blank frame. The package cannot ship a photograph: the sample image it tests with is third-party under no stated licence, which is why it is fetched and not committed. Honesty landed; representativeness did not.
  THE BOX'S OWN FIGURES DO NOT REPRODUCE AND ARE NOT REPEATED. Measured 2026-09-16 (yolo11n / pytorch / cpu, median of 15): the blank frame yields 0 boxes at 50.31 ms, a real photograph 5 boxes at 63.63 ms — 13.3 ms, ~21% of frame time, excluded from the ranking. The recorded "0.15 ms ... 100% of the 176–281 ms" is a different machine on a different day. What reproduces is the kind of error, not its magnitude — Q11 again.
  The flag lives on the profile rather than in a sweep-time warning because `src/yowo/engine.py:205` loads that profile and applies the winner to production inference long after any warning has scrolled away; `load_profile` reads it with `.get(..., False)`, so profiles written before the field existed still load and read as what they were. Gated PASS at authority human, 14 checks bound, after the gate REFUSED once: A13 carried a probe result and no check named it. Filed as a method lesson — sweep ASSUMPTIONS ids against the union of `covers:` before freezing, mechanically.
- [x] The feature cache's **recall** failure mode is measured, bounded and documented at every place a user meets the feature; **the smallest change the fingerprint cannot see is stated as a number**   (← feature-cache-honesty) · AMENDED 2026-09-13 — the original wording was ALREADY TRUE IN BOTH CLAUSES and would have been ticked for zero work while the defect shipped. Clause 1: `test_preset.py:236/266/274` assert `cfg.cache is True` and `:294/:354` assert `is False` — the default is already pinned in both directions. Clause 2: the multi-stream auto-disable IS documented at `docs/user-guide.md:1242` and `:2449` and warns at `pipeline/__init__.py:211-214`. What ships unchanged: the fingerprint is the per-channel spatial mean (3 numbers per image) with NO shape check in `check_and_load`, so — measured — a 64x64 white object appearing on a grey frame is a cache HIT and is never detected (blind spot: 91x91 px at max contrast, ~166x166 at realistic contrast), a 640x640 entry answers a 320x320 query with stale 80x80 features, and a B=1 entry answers a B=4 query by broadcasting. The guard that would stop all three exists as DEAD CODE — `_similarity.py:27`'s shape check has 0 references in `src/`. RESIDUAL: the README's "60–85% compute savings" has no experiment doc behind it, unlike every other headline figure. SIZE: standard → deep.
  TICKED 2026-09-16 (PR #73, #74) against all four of its literal clauses, each checked before the box was touched.
  MEASURED. `scripts/measure_feature_cache.py` and a dated `docs/experiments/2026-09-16-feature-cache-recall-and-savings.md`, which is the convention every other headline figure here already followed and this one did not.
  BOUNDED, which is the clause that required real work. Three of the four failures had NO bound at all and are now refused before any distance is computed: a 640x640 entry answered a 320x320 query and returned 80x80 features for an input needing 40x40; a B=1 entry answered a B=4 query because `np.abs((1,3) - (4,3))` BROADCASTS rather than raising — and `cache=True` shipped in a preset that also sets `batch_size=4`, so any video whose frame count is not a multiple of 4 ends on a partial batch; and a black-over-white frame matched a uniform grey one exactly, both averaging 0.5. The fourth, the spatial blind spot, is bounded rather than removed: per-cell means over an 8x8 grid compared by the WORST cell, from an integral image with linspace edges so every pixel lands in exactly one cell whatever H and W are. Worst placement, straddling a cell corner: 90x90 -> 22x22 px at max contrast, 116x116 -> 28x28 at 0.30, 165x165 -> 40x40 at 0.15. About 8x per side, 64x by area.
  STATED AS A NUMBER, at five surfaces — `README.md`, `src/yowo/cache/README.md`, `docs/user-guide.md` (three separate places), `config.py`'s `cache` docstring, and `src/yowo/pipeline/README.md` — with a section-scoped check that fails if any regresses.
  THE GUARD EXISTED AND NEVER RAN. `_similarity.py:26` returned 1.0 on a shape mismatch and `frame_similarity` had ZERO callers in `src/`. It was not untested dead code — `test_cache.py` tested it. That is how a shape guard stayed covered, passing and unreachable while the live path had none, and it is the sharpest example this milestone has produced of a test keeping dead code green.
  THE SAVINGS CLAIM WAS NOT MERELY UNBACKED, IT WAS BACKWARDS. Measured through the real backend path (the cache is only consulted when a frame carries a source id, `engine.py:1030-1033`, so `detect()` on a bare array never reaches it and measuring that way reports 0%): cpu 0.01 -16.6%, cpu 0.05 +44.1%, cpu 0.10 +52.4%, mps 0.01 -211.3%, mps 0.05 -92.7%, mps 0.10 -84.1%. "60-85%" is not reachable at ANY threshold measured; the best is +52.4%, CPU only, at a 130x130 px blind spot — roughly the hole the old fingerprint had to begin with. Hit rate and blindness are one number seen from two sides, and the README stated only the flattering side. Every surface now states both.
  A DEFAULT WAS MEASURED HARMFUL AND REMOVED. `cache=True` is gone from `(APPLE_SILICON, VIDEO)`, where enabling it cost 211% more time per frame — a hit copies ~6.4 MB of neck features host-to-device and a miss copies them back, so a faster device loses harder. A unit test had asserted `cfg.cache is True` there, pinning a default that made the device slower.
  THREE DECISIONS, ALL ANSWERED BY THE USER — unlike the previous node, where three timed out and were taken in silence. The third was asked DURING Build because the measurement contradicted the premise of the first: the cache had been kept on by default when the stated cost was "fewer hits", and the MPS numbers did not exist yet. Filed as a method lesson.
  RESIDUALS, named rather than buried. (a) `(CUDA_HIGH, VIDEO)` KEEPS `cache=True` and is UNMEASURED — there is no CUDA device here, and changing a default on a guess is what this milestone exists to stop; the test pins Apple Silicon and deliberately asserts nothing about CUDA. (b) The bound is a bound: a difference spread thinly enough to leave every cell mean intact is invisible to any pooled fingerprint, and the docs say so rather than implying the hole is closed. (c) One model, one machine, one image, 40 frames — what reproduces is the shape of the trade, not the percentages. (d) A2 predicted "if a fifth surface exists the box is true of four and false of the feature"; one did, and every check read a hand-written list that could not fail on a file it never opened, so a check now enumerates the repository instead — it found a sixth on its first run.
- [ ] The documented export return type is the one the function returns, **and every documented signature and CLI flag exists**   (← export-result-contract) · AMENDED 2026-09-13: there are THREE incompatible contracts, not two. `export_model` returns `ExportMetadata` (20 fields); `yowo.types.ExportResult` (7 fields) is in `yowo.__all__`, unit-tested, and returned by NOTHING; `export/README.md:56` documents a third, different `ExportResult(file_path, format, metadata)`. The drift extends past the type: the README documents `target_format: str` (real: `ExportFormat`), `output_dir` optional with a `~/.yowo/exports/` default (real: required positional, no default), `dynamic_batch=False` (real: `True`), and `cli/README.md:83` documents a `--json` flag that `yowo export --help` does not list. BLOCKED: removing a name from `yowo.__all__` is a public-API break that m4's own SCOPE defers to m5's deprecation policy. SIZE: quick → standard.
## CLOSE
evidence: <one row per task>
