---
type: Interview
task: /tasks/precision-plumbing.md
by: Tin Dang
at: 2026-09-15
interview: sha256:2d28c9cb2473959e
---
# interview 2 — precision-plumbing

## A1 [who]
- asked: that no actor distinction exists — every one of these is an in-process library call, and no caller identity changes which precision a device can execute
- if wrong: if an actor dimension did exist here it would belong on the CLI, not on a factory, and a rule written on the wrong layer would be unenforceable.
- answered: confirm

## A2 [who]
- asked: covers: S3 · the request does not say whose decision a PUBLIC type change is; RULED BY HUMAN 2026-09-15 — not this node's, and not m5's either, because the change is no longer made: `HealthReport.precision_current` stays `str` and the honesty is bought at the value, not at the type · probe: `HealthReport.precision_current` is still annotated `str` and no public `__all__` entry is removed -> if a type change were smuggled in anyway, a public consumer doing string ops on the field breaks on an upgrade that promised only a bug fix.
- if wrong: if a type change were smuggled in anyway, a public consumer doing string ops on the field breaks on an upgrade that promised only a bug fix.
- answered: confirm

## A3 [which]
- asked: the strict reading — a backend honours P only if its own runtime compute uses P — so PyTorch honours FP32 always and FP16 on CUDA, and the four artifact backends honour NOTHING, `fp32` included · found: `_onnx.py:39`, `_tensorrt.py:43`, `_openvino.py:89` and `_coreml.py:31` each take `hw_profile` alone and load a pre-compiled artifact, and only `_pytorch.py:354` has a runtime precision branch · probe: an explicit fp16 is executed on a cuda device and refused on cpu and on mps
- if wrong: an `fp32` carve-out reports one number while a FP16 TensorRT engine executes another, which is the int8 defect wearing a quieter hat.
- answered: confirm

## A4 [which]
- asked: `None` — "this is a property of the artifact I loaded, not of me" — rather than any concrete `Precision`
- if wrong: a concrete guess is R:REPORTS_REQUEST with a new spelling.
- answered: confirm

## A5 [which]
- asked: `health_report()` as the only one in this node, because the milestone's own enumeration named surfaces precision does not occupy · found: `EngineMetrics` (`metrics/_collector.py:23-47`) has no precision field, so `export_metrics()` and the Prometheus text never carried one
- if wrong: chasing the milestone's original list would have edited three surfaces that never had the defect and missed the one that did.
- answered: confirm

## A6 [which]
- asked: the three that originate with a human — the `precision=` argument, `--precision` when it is not `auto`, and `YOWO_PRECISION` — and NOT the tune profile · found: `_main.py:253` maps `--precision auto` to `None`, `config.py:436`/`:579` map the env var, `config.py:818` states presets do not set precision, and `engine.py:175-176` is the only non-human writer · probe: a tune profile carrying a non-default precision leaves config.precision None
- if wrong: leaving the profile in makes "explicit" undecidable, and the loudest rung then fires on a value nobody typed.
- answered: confirm

## A7 [which]
- asked: exactly one — a recovery action that CHANGED engine state, so `_halve_batch_size` counts and a no-op does not · found: `engine.py:732-734` defends an unconditional count as "a failed recovery is the more important one to see", but nothing was attempted, so nothing failed · probe: a tick that changes no engine state raises degradations_total by zero
- if wrong: the no-op count is what ships today and is R:PHANTOM_DEGRADATION.
- answered: confirm

## A8 [when]
- asked: it at `load()`, after `_resolve_device`, never at construction · found: `select_backend` can return `device_type=CUDA` with `device_str="cpu"` — `_selector.py:335-345` sets CUDA from `torch_cuda_available` while `device_override` sets the string, and `_pytorch.py:413-419` resolves independently
- if wrong: judging from `BackendSelection.device_type` would call fp16 honourable on a run that executes on CPU: the same lie, one layer down.
- answered: confirm

## A9 [when]
- asked: `None`, because until the device resolves, the only value available is the request · probe: a constructed-but-unloaded PyTorch backend reports `None`
- if wrong: reporting the request pre-load reinstates the defect on every health read taken during startup.
- answered: confirm

## A10 [when]
- asked: "only after a successful load", so a health read on a STARTING engine reads `"unknown"` rather than the request it was built with — `HealthReport.status` already tells the reader the engine is not READY, so the pair is complete without the field guessing · probe: a constructed-but-unloaded engine reads `"unknown"` and a status that is not READY
- if wrong: an operator polling health during a slow load is exactly the reader who would act on a startup value.
- answered: confirm

## A11 [when]
- asked: `load()` uniformly, so the ONE comparison also covers the two paths that bypass construction · found: the fallback re-selection at `engine.py:481-488` re-derives via `select_backend(...)` with NO `precision_override`, and the `backend_instance` branch at `engine.py:270-276` hardcodes `precision=Precision.FP32` — both discard an explicit request today
- if wrong: a construction-time check would miss both.
- answered: confirm

## A12 [when]
- asked: it as tier-1's band too, since a band that satisfies tier-1 must get tier-1's recovery · found: `engine.py:691-696` `return`s after the dead tier-2 call, so tier-1 is UNREACHABLE there, and `_try_precision_fallback` sets DEGRADED without setting `_oom_recovering`, so the restore branch at `engine.py:665` can never fire and one tick latches DEGRADED permanently · probe: a tick at 0.92 halves the batch and a later tick at 0.70 restores it and reads READY
- if wrong: the node inherits three defects, not the one the context file names.
- answered: confirm

## A13 [absent]
- asked: `precision=None` as "the caller expressed no preference", identical to auto, never as a request for the default · probe: precision=None raises on no device and reaches the backend unchanged
- if wrong: if `None` were read as an explicit FP32 request, every caller that has never passed `precision` would start raising on GPU.
- answered: confirm

## A14 [absent]
- asked: it as a complete and correct answer, never an error and never a fallback trigger
- if wrong: treating it as a failure would make every ONNX load look broken.
- answered: confirm

## A15 [absent]
- asked: covers: S3 · the request does not say how S2's `None` reaches `precision_current`; mapping it at the HealthReport boundary to the documented sentinel `"unknown"`, so the Protocol stays honestly `Precision | None` while the public field stays `str` · TENSION RECORDED: this dataclass's own house style is `memory_pct: float | None` three lines above (`engine.py:108`), so `str | None` would have matched it — the human ruled against the public type change on 2026-09-15 and the sentinel is the cost of that ruling, documented in one place and used for nothing else · probe: an artifact backend that cannot determine its precision makes `precision_current` read `"unknown"`, and `"unknown"` appears in no other field -> an undocumented sentinel conflates "not mine to determine" with "the read failed".
- if wrong: an undocumented sentinel conflates "not mine to determine" with "the read failed".
- answered: confirm

## A16 [absent]
- asked: `None` — "no verdict I can make" — rather than FP32, which would be a claim about an artifact it has not opened · probe: an artifact backend's verdict is None, never fp32
- if wrong: FP32 here is the artifact-backend spelling of R:REPORTS_REQUEST.
- answered: confirm

## A17 [absent]
- asked: it as "this band has no precision recovery at all", by DELETING the rung rather than keeping a hook no check in this suite can execute · found: autocast is CUDA-only (`_pytorch.py:354`) and this suite has no CUDA, so any green for a live `set_precision` would assert on a mock · probe: no backend in the tree defines set_precision and no rung calls one
- if wrong: a tier that cannot be refuted is a tier that should not ship.
- answered: confirm

## A18 [order]
- asked: availability first — `DependencyError`/`BackendError` before `ConfigError` — because a backend that cannot be imported has no resolved device to name
- if wrong: naming a device for an uninstalled backend puts a fabricated reading in the first error an operator reads.
- answered: confirm

## A19 [order]
- asked: load-then-read as the only valid order and making a read taken mid-load answer `None` rather than the request
- if wrong: the alternative is a window in which the defect still ships, which is not a fix, it is a shorter one.
- answered: confirm

## A20 [order]
- asked: it as a verdict that DISQUALIFIES that candidate — recorded in `last_exc`, the loop moves on, and the final `BackendLoadError` names it · probe: with every candidate disqualified, the raised error mentions precision
- if wrong: swallowed, the engine falls back into a backend that cannot honour the request either.
- answered: confirm

## A21 [order]
- asked: the requested one first and otherwise the backend's native precision, never a search
- if wrong: a search makes the answer depend on threshold tables the caller cannot see.
- answered: confirm

## A22 [order]
- asked: that no rung returns before a lower rung it ALSO satisfies, so severity bands nest rather than partition · probe: the 0.90-0.95 band performs tier-1's recovery
- if wrong: the partitioning reading is what makes `[0.90, 0.95)` a dead band today.
- answered: confirm

## A23 [experience]
- asked: the operator who typed `--precision fp16 --backend onnx` on an edge box, who must learn WHICH half of the pair is wrong and what to type instead · probe: the message names the backend, the device, and the remedy
- if wrong: an error saying only "unsupported" sends them to change the flag that was right.
- answered: confirm

## A24 [experience]
- asked: the author of a sixth backend, for whom `Precision | None` must be self-explaining at the Protocol — the docstring has to say what `None` MEANS, not merely that it is allowed
- if wrong: a Protocol member whose `None` is undocumented gets implemented as FP32 by the next author, which reintroduces the defect through the extension point.
- answered: confirm

## A25 [experience]
- asked: the operator reading `health_report()` off a running service to decide whether to ship, for whom `"unknown"` is actionable — it says go look at the artifact — where a confident wrong value is not · probe: no backend makes `precision_current` read a precision it is not executing
- if wrong: today that operator reads `"int8"` from a process executing fp32 and sizes hardware on it.
- answered: confirm

## A26 [experience]
- asked: the existing user whose `--precision fp16 --backend onnx` runs today and stops tomorrow, and taking the milestone's own risk note as the licence — "users' current numbers are the wrong ones, but they are the ones they have" · probe: the refusal message states that the requested precision was not being executed before either
- if wrong: a raise is cheap to soften into a warning later and expensive to harden later, so the loud direction is the reversible one.
- answered: confirm

## A27 [experience]
- asked: the author of a sixth backend, who must be FORCED to record a verdict rather than inherit a default · probe: a `BackendType` member with no verdict fails a check
- if wrong: a default verdict lets a new backend ship claiming whatever the table's fallback says.
- answered: confirm

## A28 [experience]
- asked: the operator whose alert threshold is "any degradation", for whom a count that ticks without a cause is an alert that gets muted, and a muted alert is an absent one
- if wrong: this is why a phantom count is worse than no count.
- answered: confirm

## R:REPORTS_REQUEST [reject]
- asked: A precision is reported that no backend executes — the shipping `int8`-on-CPU defect, restated as a rule so a check must kill it
- answered: confirm

## R:SILENT_COERCION [reject]
- asked: An EXPLICIT request is answered with a different precision and no error
- answered: confirm

## R:AUTO_RAISES [reject]
- asked: An auto-selected precision raises, converting a working default path into a failure
- answered: confirm

## R:SWALLOWED_BY_FALLBACK [reject]
- asked: The backend-fallback loop at `engine.py:455-496` re-derives or discards the explicit request and lands on a backend that does not honour it
- answered: confirm

## R:PHANTOM_DEGRADATION [reject]
- asked: `degradations_total` is incremented for a recovery that did not happen
- answered: confirm

## R:PUBLIC_BREAK [reject]
- asked: A public NAME disappears OR a public TYPE changes. This node removes nothing and widens nothing: `HealthReport.precision_current` stays `str`, `TuneProfile.precision` stays. If a `__all__` entry or either annotation moves, the node has taken m5's decision for it
- answered: confirm

## R:DEFAULT_FIXTURE [reject]
- asked: A check is built from the DEFAULT precision, where a value that never travelled compares equal to one that did (Q4)
- answered: confirm

## R:MOCK_ONLY_HONOUR [reject]
- asked: M1 is proved by asserting a mock received an argument, with nothing showing the backend's own numerics changed
- answered: confirm
