---
type: Interview
task: /tasks/integration-tier-revival.md
by: unrecorded
at: 2026-09-16
interview: sha256:86ad6bb6e7dabbe0
---
# interview 1 — integration-tier-revival

## A2 [which]
- asked: "every `test_*.py` under `tests/`, unit and integration alike"
- if wrong: the chromadb unit module is inside a job's path and still never executes, so an integration-only guard would call it covered
- answered: confirm

## A3 [when]
- asked: "a job in a workflow with a `pull_request` trigger names it, by path or by directory" · probe: the guard must find today's four
- if wrong: found 2026-09-16: `test_cli_e2e.py` 21 tests, `test_chroma_gallery_persistence.py` 8, `test_onnx_provider_honoured.py` 2, `tests/unit/test_chroma_gallery.py` 11 skipped at collection. All 31 integration tests pass in 10.52 s; all 11 unit tests pass in 2.73 s.
- answered: confirm

## A4 [absent]
- asked: "the check fails, naming the module and the fix"
- if wrong: a guard that reports a count teaches nobody which file to wire
- answered: confirm

## A6 [experience]
- asked: "someone who just added a test file"; the message names the module and says to add it to a job
- if wrong: the failure arrives at the moment the fix is cheapest
- answered: confirm

## A8 [which]
- asked: "by subject, not by convenience" — `test_onnx_provider_honoured.py` joins `backend-smoke` because both assert a real backend honours a request and that job already restores the weight store; `test_cli_e2e.py` takes its own name because 21 tests failing under someone else's label is a diagnosis nobody can read
- if wrong: a grab-bag job name makes every failure an investigation
- answered: confirm

## A9 [when]
- asked: covers: S2 · the request does not say the budget; taking the declared step timeout · found 2026-09-16: 31 tests in 10.52 s locally, so the budget is dominated by install and cache restore, not by the tests
- answered: confirm

## A10 [absent]
- asked: "`CI=true` fails, local skips" — the rule `tests/integration/conftest.py` already applies · probe: `test_cli_e2e.py` needs `sample_image_path` and `sample_image_dir`, `test_onnx_provider_honoured.py` needs `verified_weight`
- if wrong: found 2026-09-16, so both jobs must restore `~/.cache/yowo/test-assets` as well as the weight store
- answered: confirm

## A12 [experience]
- asked: published names registered in `FROZEN_JOB_IDS`, `docs/ci-required-checks.md` and `docs/branch-protection.json` in the same commit
- if wrong: `Export Parity` shipped blocking nothing because it appeared in one of the three
- answered: confirm

## A14 [which]
- asked: "`--extra chromadb`, the extra `pyproject.toml` already declares at line 48"
- if wrong: declaring a new dependency would touch a sensitive path to solve a problem an existing extra already solves
- answered: confirm

## A15 [when]
- asked: "every job that runs a chromadb-dependent module, and the release gate too" · probe: installing is cheap
- if wrong: found 2026-09-16: chromadb 1.5.2, 78 packages, resolves in 176 ms warm; the 11 unit tests then run in 2.73 s
- answered: confirm

## A16 [absent]
- asked: "under `CI=true` the tests must FAIL rather than skip"
- if wrong: `importorskip` is precisely the construct that converts an absent dependency into a green report
- answered: confirm

## A18 [experience]
- asked: "a check binds ci.yml and release.yml to the same extras"
- if wrong: M4 is otherwise a convention, and the four quality COMMANDS already needed a check to stay identical
- answered: confirm

## R:UNRUN_MODULE [reject]
- asked: a test module is never added without a job that runs it
- answered: confirm

## R:GREEN_BY_SKIP [reject]
- asked: a suite never reports success because its dependency was absent
- answered: confirm

## R:GUARD_BY_SPELLING [reject]
- asked: the guard never matches a module by a literal it happens to contain, only by what a job actually runs
- answered: confirm

## R:DIVERGENCE [reject]
- asked: the release path never installs less than the pull-request path
- answered: confirm
