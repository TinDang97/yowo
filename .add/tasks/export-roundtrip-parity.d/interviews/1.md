---
type: Interview
task: /tasks/export-roundtrip-parity.md
by: unrecorded
at: 2026-09-15
interview: sha256:2d31fa5fd91f58f8
---
# interview 1 — export-roundtrip-parity

## A2 [which]
- asked: "a count mismatch is a failure in its own right, asserted before any pairing"
- if wrong: `zip` silently truncates to the shorter list, so a backend that dropped a detection would be compared on the ones it kept and pass
- answered: confirm

## A3 [when]
- asked: covers: S1 · the request does not say where the bound falls; taking the box's own numbers, **1e-3 px on coordinates and 1e-4 on confidence** · probe: every variant must land inside both · found 2026-09-15, ALL TEN variants, EP pinned to CPUExecutionProvider, bus.jpg, fp32, 5 detections each. Coordinates (px): 11n 1.2207e-04 · 11s 1.2207e-04 · 11m 1.2207e-04 · 11l 9.155e-05 · 11x 1.2207e-04 · 26n 3.0899e-04 · 26s 2.4414e-04 · 26m 2.7466e-04 · 26l 1.8311e-04 · 26x 1.2207e-04 — range 9.155e-05 to 3.0899e-04, worst case 3.2x inside 1e-3, and REPRODUCING the 2026-09-13 range exactly. Confidence: 1.43e-06 · 2.4e-07 · 3.0e-07 · 5.4e-07 · 4.8e-07 · 2.09e-06 · 1.8e-07 · 2.4e-07 · 4.8e-07 · 7.2e-07 — worst 2.09e-06, **48x inside 1e-4**. The box's confidence bound is SATISFIABLE, which was NOT known before this measurement: nothing in the repo had ever compared the confidence axis. YOLO26 deviates ~2x more than YOLO11 on coordinates at every size, which is a real family difference and not noise, but every variant is comfortably inside one bound — so the per-variant tolerance the box's wording implies stays unnecessary.
- answered: confirm

## A4 [absent]
- asked: "an empty `active_providers` fails the check"
- if wrong: the property returns `()` when no session is bound, and `() != ("CPUExecutionProvider",)` must read as "could not confirm", not as a pass
- answered: confirm

## A5 [order]
- asked: "sort by descending confidence", the rule `test_backend_conformance.py:147` already established
- if wrong: output order is not part of the contract, so pairing by position compares unrelated boxes
- answered: confirm

## A6 [experience]
- asked: "someone who must decide whether the exporter regressed or the bound was always too tight"
- if wrong: a bare `assert dev <= 1e-3` tells them neither the variant nor the number
- answered: confirm

## A8 [which]
- asked: all four — yolo11n, yolo11s, yolo26n, yolo26s
- if wrong: "n/s" read as one family would leave a whole architecture unchecked on pull requests
- answered: confirm

## A9 [when]
- asked: covers: S2 · the request does not say the budget; taking "inside the existing per-job ceiling" · probe: the four PR-CI variants export and compare inside it · found 2026-09-15: four variants measured end to end locally, export dominating
- answered: confirm

## A10 [absent]
- asked: "`CI=true` fails, local skips", the rule `tests/integration/conftest.py` already applies
- if wrong: Q3: a skipped test is green
- answered: confirm

## A12 [experience]
- asked: "a published name a contributor can find in the checks list and a maintainer can require"
- if wrong: see `docs/ci-required-checks.md`, which now demands a verdict for every ci.yml job
- answered: confirm

## A14 [which]
- asked: the five sizes of both families, the registry's full detection surface
- if wrong: a schedule that silently covers nine is worse than one that covers four and says so
- answered: confirm

## A15 [when]
- asked: a daily cron
- if wrong: a weekly schedule means a regression can sit undetected for six days, and the run is cheap enough that daily costs little
- answered: confirm

## A16 [absent]
- asked: "it fails the workflow run, visibly"
- if wrong: a scheduled job nobody watches is the same shape as an advisory check, the state `docs/ci-required-checks.md` was just amended to end
- answered: confirm

## A18 [experience]
- asked: "a NEW workflow file"
- if wrong: `ci.yml`'s job ids are frozen by `test_ci_contract.py::test_ci_job_ids_are_the_frozen_set`, and this repo has no `schedule:` or `cron:` anywhere (checked 2026-09-15), so there is nothing to extend
- answered: confirm

## R:UNPINNED_PROVIDER [reject]
- asked: parity is never measured through a provider the test did not pin and confirm
- answered: confirm

## R:SELF_RATIFYING_BOUND [reject]
- asked: a bound is never derived, widened or rounded from the deviation the run just measured
- answered: confirm

## R:GREEN_BY_SKIP [reject]
- asked: the schedule never reports success because a weight, an export or onnxruntime was absent
- answered: confirm

## R:COUNT_BLIND [reject]
- asked: agreement is never asserted over a zip of two lists whose lengths were not compared first
- answered: confirm
