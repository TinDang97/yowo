---
type: Interview
task: /tasks/ci-matrix.md
by: unrecorded
at: 2026-09-16
interview: sha256:949bb61a9a3c1fb3
---
# interview 1 — ci-matrix

## A2 [which]
- asked: "exactly the classifiers, cross-checked against the `requires-python` floor" · probe: they must agree
- if wrong: found 2026-09-16: classifiers name 3.8-3.12 and `requires-python = ">=3.8"`, so the floor and the lowest classifier agree today; nothing enforces that they keep agreeing
- answered: confirm

## A3 [when]
- asked: "a job executes it on a pull request"
- if wrong: a classifier is a promise to a user, and an unexecuted promise is the whole subject of this box
- answered: confirm

## A4 [absent]
- asked: "the check fails, naming the version"
- if wrong: otherwise adding a classifier is free and silently untrue
- answered: confirm

## A6 [experience]
- asked: "someone deciding whether they can run yowo on their Python"; ONE published check name per version
- if wrong: a single rolled-up verdict cannot say WHICH version broke, which is the only thing that reader needs
- answered: confirm

## A8 [which]
- asked: "what the package documents as public: `import yowo`, the names `__init__` exports, the public submodules, the model registry, and the CLI entry point" · probe: all of it must work on the oldest claim
- if wrong: found 2026-09-16 on a real CPython 3.8.20: `import yowo` OK at 2.5.0, public API and submodule imports OK, registry returns 10 detection variants, `yowo --help` and `yowo models` both exit 0
- answered: confirm

## A9 [when]
- asked: covers: S2 · the request does not say the budget; taking a declared step timeout · found 2026-09-16: the smoke needs core dependencies only (numpy, opencv-python-headless, click, pyyaml, requests, tqdm), no torch and no onnxruntime, so a leg is an install and a few imports
- answered: confirm

## A10 [absent]
- asked: "the job fails"
- if wrong: a version whose install fails is a claim that is false, which is exactly what this job is for
- answered: confirm

## A12 [experience]
- asked: covers: S2 · the request does not say what the checks are called; taking one published name per version, each registered in `FROZEN_JOB_IDS`, `docs/ci-required-checks.md` and `docs/branch-protection.json`
- answered: confirm

## A13 [who]
- asked: covers: S3 · the request does not say who may narrow coverage; taking "a human, with the cause recorded beside the gap"
- answered: confirm

## A14 [which]
- asked: "the UNIT tier on the newest claimed version in addition to 3.11, and the claim smoke on all five" · probe: the unit tier cannot simply be matrixed
- if wrong: **found 2026-09-16, and it changes the shape of this box**: under a real CPython 3.8.20, `src/yowo` compiles 96/96 but the TEST SUITE compiles only 141/163 — 22 modules use parenthesized context managers (3.10+) — and on a real 3.10.20 all 163 compile while 6 modules `import tomllib`, which is 3.11+ stdlib. The test suite's floor is therefore **3.11**, four versions above the package's. Matrixing the unit tier over the claim is not possible without rewriting 22 test files, and the claim is about the shipped package rather than the test suite.
- answered: confirm

## A15 [when]
- asked: 3.12, the highest classifier · probe: it must actually pass
- if wrong: found 2026-09-16 on a real CPython 3.12.13: **2863 passed, 14 skipped** (the extra skips are ad-hoc-env absences, not failures)
- answered: confirm

## A16 [absent]
- asked: "recorded as a named gap with its cause" — human decision 2026-09-16, asked and answered: record the gap, add no OS job
- if wrong: `Backend Conformance` already carries a strict xfail scoped to macOS arm64 for an unexplained PyTorch-vs-ONNX divergence owned by m4, so adding macOS to the integration tier would go red on arrival, which is precisely what this box's own wording warns against
- answered: confirm

## A18 [experience]
- asked: "a check that fails if the recorded gap disappears"
- if wrong: a limitation kept only in prose stops being read
- answered: confirm

## R:HANDLIST [reject]
- asked: the matrix is never a literal list that can drift from the classifiers
- answered: confirm

## R:CLAIM_UNRUN [reject]
- asked: a version is never claimed in metadata while no job executes it
- answered: confirm

## R:DEVENV_PROVES_SHIP [reject]
- asked: a claim about the shipped package is never evidenced by a run that installed the dev group
- answered: confirm

## R:SILENT_GAP [reject]
- asked: an uncovered platform is never left implied by a missing job
- answered: confirm
