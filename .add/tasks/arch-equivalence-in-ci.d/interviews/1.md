---
type: Interview
task: /tasks/arch-equivalence-in-ci.md
by: unrecorded
at: 2026-09-16
interview: sha256:c12eae8a8093d133
---
# interview 1 — arch-equivalence-in-ci

## A2 [which]
- asked: "the raw model output, before any postprocessing"
- if wrong: comparing post-NMS detections would measure yowo's postprocess against ultralytics' postprocess, which is a different claim from architectural equivalence and would let an arch error hide behind an NMS difference
- answered: confirm

## A3 [when]
- asked: covers: S1 · the request does not say where the bounds fall; taking two, measured first · probe: every variant lands inside both · found 2026-09-16 on bus.jpg, fp32, same weight file both sides — YOLO11 raw over all 8400 anchors: box 5.6458e-04..**1.4038e-03** px, class 1.3188e-06..**3.5942e-05**; YOLO26 end2end over real detections: box 3.0518e-05..6.1035e-05, conf 1.1921e-07..1.7881e-06. **A single 1e-3 bound would FAIL yolo11l at 1.4038e-03** — the trap for anyone reusing the parity suite's constant.
- answered: confirm

## A4 [absent]
- asked: "fails under CI, skips locally"
- if wrong: ultralytics is a dev dependency gated on python>=3.11, so a 3.10 runner would otherwise report equivalence it never checked
- answered: confirm

## A5 [order]
- asked: "by position for YOLO11's anchor-ordered pre-NMS output, and by confidence rank over real detections for YOLO26's end2end output" · probe: the two layouts must be handled differently
- if wrong: measured 2026-09-16: position-pairing YOLO26's (1,300,6) top-300 list on a random-noise input reports a max deviation of **6.479e+02**, which is not a defect but junk rows reordering. The same comparison on a real image with real detections gives 3.0518e-05. Pairing is the whole difference between a phantom bug and a measurement.
- answered: confirm

## A6 [experience]
- asked: "someone deciding whether the architecture drifted or the oracle moved"
- if wrong: the message must name the variant, the axis, the value, the bound and the ultralytics version
- answered: confirm

## A8 [which]
- asked: "all ten" · probe: the whole set must fit the job budget
- if wrong: a subset would leave a family or a size unchecked on the branch that ships
- answered: confirm

## A9 [when]
- asked: covers: S2 · the request does not say the budget; taking the job's declared timeout · found 2026-09-16: the ten-variant comparison runs locally in the same order as the parity suite's ten, dominated by loading each weight twice
- answered: confirm

## A10 [absent]
- asked: "`CI=true` fails, local skips" — the rule `tests/integration/conftest.py` already applies
- if wrong: Q3: a skipped test is green
- answered: confirm

## A12 [experience]
- asked: "`Arch Equivalence`, published and registered in `docs/ci-required-checks.md` in the same commit"
- if wrong: `test_every_pull_request_check_is_classified_as_gating_or_advisory` now refuses an unclassified job, which is how `Export Parity` shipped blocking nothing
- answered: confirm

## A13 [who]
- asked: covers: S3 · the request does not say who may move a bound; taking "a human in a reviewed commit, never to admit a deviation the run just produced"
- answered: confirm

## A14 [which]
- asked: "box geometry in pixels, and class score / confidence"
- if wrong: these differ by two orders of magnitude and one bound over both would be set by the looser
- answered: confirm

## A15 [when]
- asked: covers: S3 · the request does not say when a bound may tighten; taking "freely, in the commit that earns it"
- answered: confirm

## A16 [absent]
- asked: "the check fails"
- if wrong: deleting a constant must not read as satisfying it
- answered: confirm

## A18 [experience]
- asked: "recorded beside the bounds as documentation, never asserted"
- if wrong: Q11: a number measured on one machine is not a property of the code, and this repo has already had CI reject a pinned per-machine figure
- answered: confirm

## R:ORACLE_NOT_RUN [reject]
- asked: equivalence is never asserted against a hardcoded expected tensor or a number copied from a previous run
- answered: confirm

## R:POSITION_PAIRED [reject]
- asked: an order-dependent output is never compared row-by-row by position
- answered: confirm

## R:GREEN_BY_SKIP [reject]
- asked: the job never reports success because ultralytics, a weight, or a variant was absent
- answered: confirm

## R:BOUND_FITTED_TO_RESULT [reject]
- asked: a bound is never widened to admit a deviation the run just produced
- answered: confirm
