---
type: Interview
task: /tasks/map-regression-gate.md
by: unrecorded
at: 2026-09-15
interview: sha256:0e0f009cce2511b2
---
# interview 1 — map-regression-gate

## A2 [which]
- asked: "exactly the images the run evaluated" · probe: `run_benchmark(subset=500)` must report the scoped number, not the full-ground-truth one
- if wrong: a gate on the wrong scope rewards a model that stops detecting, the exact defect PR #49 fixed one layer down · found 2026-09-15: `run_single_backend:242` calls `evaluate_coco_map(gt_ann_path, coco_results)` with no scope, so the shipped `yowo benchmark --subset 500` reports **0.0419** where the truth is **0.4158** — 4500 of 5000 ground-truth images counted as total misses. The repair landed in the evaluator and stopped at the runner's door.
- answered: confirm

## A3 [when]
- asked: "clamp, and report the count actually evaluated"
- if wrong: a silent clamp that still reports the requested N misattributes the denominator
- answered: confirm

## A4 [absent]
- asked: "omitted scope evaluates every ground-truth image", preserving today's full-dataset behaviour exactly
- if wrong: changing the no-argument meaning would silently rescope every existing caller
- answered: confirm

## A5 [order]
- asked: `sorted(getImgIds())[:N]`
- if wrong: unsorted ids come out of the JSON in file order and the "same" subset differs per machine. NOTE: this rule is now written out in THREE places — `_evaluator.py:199` (evaluate), `_evaluator.py:313` (load), `tests/support/datasets.py:699` (`select_subset_ids`, whose own docstring says it mirrors the evaluator). Passing the evaluated ids instead of re-deriving them collapses two of the three.
- answered: confirm

## A6 [experience]
- asked: "a contributor reading a benchmark table, who has no way to tell 0.0419 from a real regression"
- if wrong: the defect above shipped precisely because a wrong number looks exactly like a right one
- answered: confirm

## A7 [who]
- asked: "a human, in a reviewed commit, never the gate itself"
- if wrong: a gate that rewrites its own baseline records history and prevents nothing
- answered: confirm

## A8 [which]
- asked: M2's list
- if wrong: a bare float cannot distinguish "the model regressed" from "the weights were re-pinned"
- answered: confirm

## A9 [when]
- asked: covers: S2 · the request does not say when a baseline goes stale; taking "when any recorded input changes", which the recorded digests make checkable rather than remembered
- answered: confirm

## A10 [absent]
- asked: "the gate fails"
- if wrong: "no baseline, therefore pass" is R:GREEN_BY_SKIP with extra steps
- answered: confirm

## A12 [experience]
- asked: covers: S2 · the request does not say what the reader needs; taking "the file must be readable without running anything" — JSON with the provenance inline, not a pickle or a bare number
- answered: confirm

## A14 [which]
- asked: "yolo11n · PyTorch · CPU" — the one combination CI can actually run, and the combination the baseline records · probe: the recorded backend and device must match what the gate's own run reports
- if wrong: a baseline measured on one backend and enforced on another measures the backend, not the model
- answered: confirm

## A15 [when]
- asked: covers: S3 · the request does not say the runtime budget; taking "the existing Accuracy Dataset job's 45-minute ceiling" · found 2026-09-15: 500 images inferred in **20.8 s** on local CPU, plus a cached ~1.07 GB dataset restore — the gate is cheap enough to run on every pull request
- answered: confirm

## A16 [absent]
- asked: "`CI=true` turns a missing dataset from a skip into a failure", the rule `test_coco_dataset.py` already established
- if wrong: Q3: a skipped test is green
- answered: confirm

## A18 [experience]
- asked: "the message names the baseline, the measurement, the delta and which recorded input to check first"
- if wrong: a bare `assert 0.41 >= 0.42` sends the reader to the source to learn what the gate even measured
- answered: confirm

## R:UNSCOPED_EVALUATION [reject]
- asked: mAP is never computed against a ground-truth image set derived independently of the predictions' own image list
- answered: confirm

## R:SELF_UPDATING_BASELINE [reject]
- asked: the gate never writes the baseline it is gating against
- answered: confirm

## R:GREEN_BY_SKIP [reject]
- asked: the gate never reports success because the dataset, the weights or pycocotools were absent
- answered: confirm
