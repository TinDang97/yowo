---
type: Interview
task: /tasks/export-fp16-default.md
by: Tin Dang
at: 2026-09-13
interview: sha256:47e993710fea7633
---
# interview 1 — export-fp16-default

## A1 [who]
- asked: n/a · export is a local developer action with no actor distinction; no caller identity affects the artifact.
- answered: confirm

## A2 [which]
- asked: all three that ship a CLI verb — detection, OBB, classification — since the CLI offers no kind it cannot export · probe: all three exit 0 and write files
- if wrong: a kind left broken ships a default that fails for some users.
- answered: confirm

## A3 [when]
- asked: n/a · no time or ordering boundary; export is a single synchronous call.
- answered: confirm

## A4 [absent]
- asked: the signature default `Precision.FP16` as the value under test, not a substitute · probe: the default is still FP16 when the artifact check passes
- if wrong: if the default silently became FP32 the check would go green over an unfixed FP16 path.
- answered: confirm

## A5 [order]
- asked: n/a · single call, no tie to break.
- answered: confirm

## A6 [experience]
- asked: the user running the documented command from the README with no flags
- if wrong: that user currently gets exit 1 and an empty directory with no indication the default is the problem.
- answered: confirm

## R:DEFAULT_NARROWED [reject]
- asked: The default precision is changed to a value that already worked, making the check pass without repairing FP16
- answered: confirm

## R:MOCKED_EXPORT_PROOF [reject]
- asked: M1 or M2 is proved with `_export_onnx` patched. Every `export_model` call in the unit suite mocks the exporter and passes FP32 or INT8 — never the default — which is exactly why this shipped
- answered: confirm
