---
type: Interview
task: /tasks/sidecar-not-self-attesting.md
by: Tin Dang
at: 2026-09-10
interview: sha256:123ec1bbc42b451a
---
# interview 1 — sidecar-not-self-attesting

## A1 [who]
- asked: any principal that can write `~/.cache/yowo/weights/` — a second local account, a shared CI cache, a synced directory, a restore from backup — and NOT a remote attacker, since the sidecar is never downloaded
- if wrong: and only root is in scope, the fix is unnecessary work that costs one hash per load.
- answered: confirm

## A2 [which]
- asked: every sidecar `load_verified_state_dict` reads, pinned and unpinned alike, since a single trusted branch is the whole hole
- if wrong: an unpinned path pays for a guarantee nobody asked it for.
- answered: confirm

## A3 [when]
- asked: the raw hash MUST be computed before the sidecar's contents are read back, not merely before they are returned
- if wrong: and the check lands after the read, a malformed sidecar is deserialized on attacker terms before anything rejects it.
- answered: confirm

## A4 [absent]
- asked: it as an error — a sidecar alone is not a loadable model, and letting it serve is exactly R:SELFKEYED with the file deleted
- if wrong: a user who deletes the `.pt` to save disk and keeps the sidecar loses a workflow that works today.
- answered: confirm

## A5 [order]
- asked: raw-digest-first — hash the file, then compare that hash to the sidecar's claim AND to the pin, so one hash serves both questions and the pin never substitutes for the measurement
- if wrong: and the pin is consulted first, M3 and M1 cannot both hold.
- answered: confirm

## A6 [experience]
- asked: the operator running inference, who needs to know the cache is untrustworthy rather than that a hash differed — the message must say which file, that it was discarded, and that it is being reconverted
- if wrong: a routine stale-sidecar reconversion reads as a break-in.
- answered: confirm

## R:SELFKEYED [reject]
- asked: A sidecar authenticated by a value that is public, caller-supplied, or stored in the sidecar itself.
- answered: confirm

## R:SILENTSERVE [reject]
- asked: A mismatch that returns tensors anyway, on any branch.
- answered: confirm

## R:REHASH [reject]
- asked: A repaired path that hashes the raw checkpoint more than once for one load.
- answered: confirm
