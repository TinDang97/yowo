---
type: Interview
task: /tasks/pytorch-onnx-numeric-divergence.md
by: Tin Dang
at: 2026-09-13
interview: sha256:d124fd7cac04ef07
---
# interview 1 — pytorch-onnx-numeric-divergence

## A1 [who]
- asked: n/a · no actor distinction; the EP is chosen per-session from the caller's own device argument.
- answered: confirm

## A2 [which]
- asked: anything that is not `"auto"` — `"cpu"` and any `"cuda*"` — since `"auto"` is the only value that delegates the choice · probe: `"cpu"` yields only the CPU EP while `"auto"` still yields CoreML
- if wrong: if `"auto"` were treated as explicit the speedup would be lost for every default caller.
- answered: confirm

## A3 [when]
- asked: n/a · selection happens once at load; there is no re-selection boundary.
- answered: confirm

## A4 [absent]
- asked: an empty tuple rather than None, so a caller can iterate unconditionally · probe: the property is safe to read on an unloaded backend
- if wrong: a None would make the honest-reporting path raise on the error path that most needs it.
- answered: confirm

## A5 [order]
- asked: ORT's own convention that earlier entries win, so the CPU EP alone means the CPU EP runs
- if wrong: a trailing fallback that silently wins would reproduce the defect this node fixes.
- answered: confirm

## A6 [experience]
- asked: covers: S1, S2 · the request does not say who meets this; taking the macOS user who passes `device="cpu"` expecting CPU numerics and today gets the Neural Engine in FP16, and the developer comparing backends who cannot tell which EP produced a number.
- answered: confirm

## R:SUBSTITUTED [reject]
- asked: An explicitly requested device is silently served by a different execution provider
- answered: confirm

## R:REQUESTEDASRAN [reject]
- asked: The reported provider list is the one passed in rather than the one read back from the session
- answered: confirm

## R:AUTOSLOWED [reject]
- asked: `device="auto"` stops selecting CoreML on a host that has it, trading the speedup for the fix
- answered: confirm
