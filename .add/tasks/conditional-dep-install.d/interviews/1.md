---
type: Interview
task: /tasks/conditional-dep-install.md
by: unrecorded
at: 2026-09-16
interview: sha256:fc288d570d1d2abd
---
# interview 1 — conditional-dep-install

## A2 [which]
- asked: "every argument to a real `importorskip` call anywhere under `tests/`" · probe: the set must be discoverable
- if wrong: found 2026-09-16: 18 modules, 8 dependencies — chromadb, torch, onnxruntime, onnx, onnxruntime.quantization, yowo.backends._onnx, openvino, pycocotools
- answered: confirm

## A3 [when]
- asked: "the dev group, plus the extras that job's `uv sync` names, resolved from pyproject.toml" · probe: a hand-map gets this wrong
- if wrong: found 2026-09-16: my own first pass mapped pycocotools to the `benchmark` extra and reported the `quality` job at risk for two modules. `pycocotools>=2.0.4` is in the dev group DIRECTLY as well, so both were fine. CI had logged exactly one skip. The hand-map produced two false positives out of three.
- answered: confirm

## A4 [absent]
- asked: "the top-level distribution name"
- if wrong: `onnxruntime.quantization` resolves through `onnxruntime`, `yowo.backends._onnx` through the package itself
- answered: confirm

## A6 [experience]
- asked: "someone deciding install-or-accept"; the message names module, dependency, job and both options
- if wrong: otherwise the cheap fix is to delete the check
- answered: confirm

## A7 [who]
- asked: covers: S2 · the request does not say who may accept a gap; taking "a human, in a reviewed commit, with the reason beside the entry"
- answered: confirm

## A8 [which]
- asked: the measured one · probe: exactly one
- if wrong: found 2026-09-16: openvino in `ci.yml:quality`, and CI's own log confirms a single skip, `tests/unit/test_backend_roster.py:177`
- answered: confirm

## A9 [when]
- asked: INSTALL
- if wrong: the skipped test guards R:FALSEDEP, a defect that shipped on openvino 2026.3.1 and sent users to install a package they already had; `conformance` already proves the extra installs on ubuntu CPU in 2 packages
- answered: confirm

## A10 [absent]
- asked: "valid, and the strongest state"
- if wrong: an empty list must never read as the check being disabled
- answered: confirm

## A12 [experience]
- asked: covers: S2 · the request does not say where an accepted gap lives; taking "beside the check, with its reason, so the diff that accepts one is reviewable"
- answered: confirm

## A14 [which]
- asked: `ci.yml:quality` and `release.yml:quality`
- if wrong: they already run four identical commands, and `integration-tier-revival` bound their extras for chromadb alone
- answered: confirm

## A15 [when]
- asked: covers: S3 · the request does not say when they may diverge; taking "never, for anything `tests/unit/` conditionally imports"
- answered: confirm

## A16 [absent]
- asked: "the check fails"
- if wrong: the release gate silently running fewer tests is the weaker-gate failure
- answered: confirm

## A18 [experience]
- asked: "a check, not a convention"
- if wrong: the four quality COMMANDS already needed one to stay identical
- answered: confirm

## R:HANDMAP [reject]
- asked: the guard never resolves a dependency through a list maintained by hand
- answered: confirm

## R:GREEN_BY_SKIP [reject]
- asked: a job never reports success over a module that skipped for want of a dependency
- answered: confirm

## R:SILENT_ACCEPT [reject]
- asked: a gap is never accepted by deleting the check or by omission
- answered: confirm

## R:TEXT_MATCH [reject]
- asked: a call is never detected by matching source text
- answered: confirm
